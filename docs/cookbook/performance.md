# Performance

This page will cover grouping choice, compact grouping, buffering, full-device loading, `jit_unroll_slices`, how memory scales with simulation shape, probe modes, and slice count, diagnosing recompilation, and verifying CPU/GPU execution.

!!! note "Planned page"
    This page is planned but not yet written. It follows the page templates in the [authoring guide](../design/authoring-guide.md). Sources: `phaser/engines/common/simulation.py`, `benchmarks/`.
