# Coordinates, units, and arrays

This page will state the array shapes, axis order, physical units, and coordinate conventions (including diffraction-pattern origin) used throughout Phaser, so later pages can link to a single definition instead of restating them.

!!! note "Planned page"
    This page is planned but not yet written. It follows the page templates in the [authoring guide](../design/authoring-guide.md). Sources: `phaser/state.py`, `phaser/utils/num.py`, `phaser/hooks/io/`.
